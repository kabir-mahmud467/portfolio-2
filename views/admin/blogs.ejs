<%- include('../partials/adminHeader') %>

<section>
  <h2>Blogs</h2>
  <a href="/admin/blogs/new">New Blog</a>
  <ul>
    <% items.forEach(item => { %>
      <li>
        <strong><%= item.title %></strong>
        <div>
          <a href="/admin/blogs/<%= item._id %>/edit">Edit</a>
          <form method="post" action="/admin/blogs/<%= item._id %>/delete" style="display:inline" onsubmit="return confirm('Delete this blog?')">
            <button type="submit">Delete</button>
          </form>
        </div>
      </li>
    <% }) %>
  </ul>
  <% if (total && limit) { %>
    <div>Page <%= page %> of <%= Math.ceil(total/limit) %></div>
  <% } %>
</section>

<%- include('../partials/adminFooter') %>
