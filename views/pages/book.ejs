<%- include('../partials/header', {
  user: user,
  title: title,
  description: description,
  ogTitle: ogTitle,
  ogDescription: ogDescription,
  ogImage: ogImage,
  ogType: ogType,
}) %>

<article class="glass-card blog-post">
  <% if (item.image) { %>
    <img class="blog-post-image" src="<%= item.image %>" alt="<%= item.title %>">
  <% } %>
  <p class="blog-post-meta"><%= purchased ? 'Purchased book' : 'Locked book' %></p>
  <h1 class="blog-post-title"><%= item.title %></h1>
  <% if (item.author) { %>
    <p style="color: #94a3b8; margin: 0;"><%= item.author %></p>
  <% } %>
  <p><%= item.description %></p>
  <p style="color: #cbd5e1;"><strong><%= formatDollar(item.price) %></strong></p>
  <div class="content-actions">
    <a class="secondary-button" href="/books/">Back to books</a>
    <% if (purchased && item.pdf) { %>
      <a class="buy-button" href="<%= item.pdf %>" target="_blank" rel="noreferrer">Open PDF</a>
    <% } else if (!purchased) { %>
      <form method="post" action="/books/<%= item.slug %>/buy" style="display:inline;">
        <button type="submit" class="buy-button">Buy to unlock</button>
      </form>
    <% } %>
  </div>
</article>

<%- include('../partials/footer') %>
